# T10 Delivery - 2026-06-23

## Scope

Updated the Figma file `25.1213 MOS v2` (`1qe8d4ll9wLnxPAsiJAoa9`) for the existing-account Google flow:

- `MOS v2 / 微官網管理 / 09 Google 有帳號-Step1` (`1866:23500`)
- `MOS v2 / 微官網管理 / 10 Google 有帳號-Step2` (`1981:28605`)
- `MOS v2 / 微官網管理 / 11 Google 有帳號-Step3` (`1981:28821`)
- `MOS v2 / 微官網管理 / 12 Google 有帳號-Step4` (`1866:24236`)

No-account Google steps `04-08` were only sampled for verification and were not modified.

## Changes

- Replaced the old `T06 / Google Yes Step Stack` structure with a single visible `T06 / Google Step Content / yes / N` block per target frame.
- Reused the corrected existing-account Step1 Google card structure for header, tabs, and stepper spacing.
- Set target frame height to `1748` and moved fixed footer controls to the new bottom.
- Preserved each step's existing business text and state-specific content.
- Normalized leftover orange Google-flow controls to the existing `Global/Colors/Brand/colorPrimary` token.
- Rebuilt Google hotspots:
  - No-account tab navigates to `1866:19820`.
  - Step circles navigate to the other existing-account step frames.
  - Step1/Step2/Step3 primary actions navigate to the next step.
  - Current-step circles intentionally have no self-navigation.

## Validation

- Final audit confirmed all four target frames have exactly one Google step content block and `0` old Google stacks.
- Final audit confirmed `0` invalid Google prototype reactions.
- Final audit confirmed no leftover warm/orange fills or strokes inside the updated Google content blocks.
- Final audit confirmed text token bindings in the Google cards:
  - Step1: 11 bound visible text nodes
  - Step2: 18 bound visible text nodes
  - Step3: 16 bound visible text nodes
  - Step4: 16 bound visible text nodes
- Visual screenshots saved:
  - `mos-v2-google-yes-step1-after.png`
  - `mos-v2-google-yes-step4-after.png`

## Notes

`get_design_context` intermittently failed with a Figma MCP transport error, but `get_metadata`, `get_variable_defs`, `get_screenshot`, and `use_figma` succeeded. The implementation used direct Figma Plugin API inspection and writes against the live file.
