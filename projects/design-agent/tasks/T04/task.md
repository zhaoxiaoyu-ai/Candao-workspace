# T04 - MOS v2 Shared Shell And Style Correction

## Status

done

## Result

Completed in Figma on 2026-06-16.

- Replaced generated one-off sidebar, breadcrumb, and footer shell visuals with shared COMS instances/patterns.
- Normalized generated frame text to Roboto.
- Restyled input labels to match the COMS `input-title` title style.
- Restyled card containers to the COMS Card radius/effect treatment.
- Preserved existing prototype navigation and modal/state reactions.

## Goal

Correct the generated MOS v2 Figma pages according to latest design feedback:

- Use Roboto consistently for all text.
- Use the shared/component form title style instead of loose manual labels.
- Align all card radius and shadow/effect styling with the existing same-system pages.
- Reuse the shared/common sidebar, breadcrumb, and bottom bar patterns instead of local one-off shell drawing.

## Source

- Target Figma file: `25.1213 MOS v2`
- File key: `1qe8d4ll9wLnxPAsiJAoa9`
- Target page: `UI（確認稿）`
- Target frames: existing 15 `MOS v2 /` frames created in T01-T03
- Reference layout: existing brand/settings pages in the same Figma file and subscribed COMS component library.

## Constraints

- Preserve the existing COMS instances and prototype interactions from T03.
- Do not modify local prototype source under `e:\Downloads\Tappo0615`.
- Prefer shared MOS/COMS instances and design tokens over detached manual styling.

## Validation

- Audit all generated text for Roboto.
- Audit form labels for shared title component/style usage.
- Audit cards for matching radius/effects.
- Audit sidebar, breadcrumb, and footer/bottom shell usage.
- Recheck prototype reactions and frame overflow.
