# Hue Skill Install Note

Date: 2026-05-22

Installed skill:

```text
dominikmartn/hue@hue
```

Install command used:

```powershell
npx skills add dominikmartn/hue@hue -g -y
```

Installed location:

```text
C:\Users\Admin\.agents\skills\hue
```

Purpose:

- `hue` is a meta-skill for generating design language skills.
- It is relevant for creating a reusable Tappo design-system skill that can produce high-fidelity, low-drift UI implementations across sessions.

Next step:

- Restart Codex to pick up the new skill in the available skills list.
