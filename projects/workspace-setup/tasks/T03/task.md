# Task T03: Convert Candao Workspace To A Monorepo

## Goal

Turn `E:\Projects\Candao-workspace` into the standalone top-level Git repository for all current Candao projects in this workspace.

## Requirements

- Do not modify or read from `E:\Projects\ai-workspace`.
- Initialize Git at `E:\Projects\Candao-workspace`.
- Preserve the existing Git metadata from nested source repositories as local backups before removing them from active use.
- Keep all current child project files under the workspace root so the new repository captures the full Candao workspace state.
- Add or update ignore rules needed for workspace-local runtime artifacts and nested Git backups.

## Result

Done on 2026-06-29.
